apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: cluster-issuer
  namespace: cert-manager
resources:
  - clusterIssuer.yaml
spec:
  dependsOn:
    - name: cert-manager
  interval: 5m
  path: "./infrastructure/certmanager/cert-manager "
